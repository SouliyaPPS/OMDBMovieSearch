<% include partials/header %>

  <div class="movie">
    <div class="movie-search">
      <h1 class="results">Results Page:</h1>

        <ul>
          <% data["Search"].forEach(function(movie) { %>
            <li>
              <strong>
                  <%= movie["Title"] %>
              </strong> -
              <%=movie["Year"]%>
            </li>
            <% }) %>
        </ul>
      <a href="/">Go Back to Search</a>
      </div>
    </div>

  <% include partials/footer %>
